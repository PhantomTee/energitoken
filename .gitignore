node_modules/
.env
.env.local

# contract
contract/cache/
contract/artifacts/
contract/typechain-types/
contract/coverage/
contract/coverage.json

# app
app/.expo/
app/dist/
app/web-build/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# firebase
firebase/firebase.config.ts
firebase/serviceAccountKey.json

# secrets scratchpad
SECRETS.txt

# OS / editor
.DS_Store
.vscode/
*.log

# PlatformIO build tree (generated; see platformio.ini)
.pio/

# Meter build credentials. The firmware reads these at compile time; they
# are kept out of the repository so the source can be published without the
# database key or the meter-signing keys. See common/secrets.h's own header
# for how to recreate it.
firmware/esp32/common/secrets.h
